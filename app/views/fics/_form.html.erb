<%= form_for(@fic) do |f| %>
  <% if @fic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fic.errors.count, "error") %> prohibited this fic from being saved:</h2>

      <ul>
      <% @fic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
